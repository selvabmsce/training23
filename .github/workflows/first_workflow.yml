name: first-action
run-name: instance
on: [push]

jobs:
  hello:
   runs-on: ubuntu-latest
   steps:
    - run: echo "Hello World"
    - name: Checkout
      uses: actions/checkout@v3.5.3
    - run: python test.py
    
